server:
  port: 8080
  servlet:
    context-parameters:
      redirect-to-https: true

springdoc:
  api-docs:
    path: /api
  swagger-ui:
    path: /swagger-ui.html
    url: https://challenge-test.online/api-docs
  paths-to-exclude: /swagger-resources/**